import Foundation

// TODO: Import Supabase Swift SDK
// import Supabase

class SupabaseClient {
  static let shared = SupabaseClient()

  private init() {
    // Initialize Supabase client with URL and API key from Config
    // let url = URL(string: Config.supabaseUrl)!
    // let key = Config.supabaseAnonKey
    // self.supabase = SupabaseClient(url: url, headers: ["Authorization": "Bearer \(key)"])
  }

  // TODO: Implement Supabase queries
  // - fetchAthletes(gymId:)
  // - fetchEvaluations(athleteId:)
  // - fetchAssessmentCriteria(gymId:)
  // - addEvaluation(_:)
  // - updateEvaluation(_:)
  // - deleteEvaluation(_:)
}
